import Header from "../Header/Header";

import { LayoutWrapper } from "./Layout.styled";

const Layout = () => {
  return (
    <LayoutWrapper>
      <Header />
    </LayoutWrapper>
  );
};

export default Layout;
